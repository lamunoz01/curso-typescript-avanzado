/*Ejemplos - Arrays NORMALES */
const numbers: number[] = [1,25,13,66,99];
//Generan mutaciones d
numbers.push(85);
numbers.pop();
numbers.unshift(1);
numbers.filter(()=>{});
numbers.reduce(()=>0)
numbers.map(()=>0)